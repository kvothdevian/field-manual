# Brief ch13 — "Shipping for $0" (a URL tonight beats localhost forever)

Status: PROPOSED — batch approval with ch12/ch14–ch16. Frozen on approval.

## Outcome promise

Ship a pre-publish gate + written deploy-readiness card for a $0
host from a scratch static site — every "ready" backed by three
reader-executed checks and a card naming host + first wall + date.

## Coverage dimensions (matrix-bound)

Static→git→connect ritual (●) · numeric vs legal walls (●) ·
agent spend budget (●) · pre-publish gate (●) · readiness card
(●) · static-host limits (●).

## Venue

Scratch repo `ship-lab/` (`index.html` 2-link toy w/ seeded broken
link + authored `gate.mjs` + `READY.md`). Executed: gate runs RED
then GREEN from reader keystrokes; host-wall fit computed on paper
(reader recomputes one row); readiness card filled docs-only —
nothing pushed, no accounts, no credentials. Book-only.

## Briefing (≤500w)

Stakes (a URL tonight beats localhost forever) → ritual vendor-free
(static folder → git → connect once, push = deploy) → two wall
species (numeric meters trip silently; legal clauses have no
dashboard) → $0 budget under an agent (N production deploys,
previews-first, stop line; previews = free rehearsal) → gate before
push (links resolve · weight fits published wall · no secret-shaped
strings; local, no account) → readiness card + breaks-down (static
hosts serve files only; free numbers move silently, re-stamp
quarterly; silence about a limit is hope, not architecture).

## Visuals — quad {svg, stat-strip, timeline, figure}

1. **Fig. 13 — the two-species wall (signature SVG).** Numeric
   meter (fills silently) vs legal clause (fires by email);
   previews-first arrow bypassing production meter. Answers:
   "which wall warns me, and which needs a terms page?"
2. **Pre-publish gate stat-strip.** LINKS n/n · WEIGHT x of y-wall
   · SECRETS 0 hits, PASS/FAIL shape+label. Doubles as steps-1/2
   expectation.
3. **Push-to-URL timeline.** folder → git → connect-once
   (docs-only badge) → preview → review → merge → production, with
   meter-spend marks on the last two nodes only.
4. **First-wall comparison figure.** Host cards with wall bars: 4
   hosts × published site/file/build wall + as-of + what bites
   first at the reader's weight. Numbers live here only.

## Session (4 steps)

1. Stage the site + written spend budget; gate runs RED.
2. Green the gate with own keystrokes; 1-file diff; outputs match.
3. Fit the host wall on paper; reader recomputes one row; UNPINNED
   written where unpinned, never infinity.
4. Fill READY.md docs-only; untouched status except 2 files; card
   holds host + numeric first-wall + as-of + ToS line.

## Evidence

Ledger ch13 rows: ALL host-number rows re-fetched live (Cloudflare
/ Vercel / Netlify / Pages limits + Pages ToS bar — highest legal
load). Repairs: preview-deployments URL-form row; Pages-about
compound row split into fetchable rows, cited only after repair.
New: secret-scan basis cross-cited to ch14/ch04 deny rows (no new
page unless the gate cites one); gate.mjs stays authored-labeled.
Cut/label any August row that fails re-verification.

## Teach test

Session ends with a dated readiness card (host + numeric
first-wall + as-of). Adversary fails "agent says deployable"
without a reader-run red→green transcript, or taste-accepts.
